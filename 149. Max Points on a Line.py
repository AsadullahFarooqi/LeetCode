"""
step 1
    find the slope between two points using
    slop = (y2-y1)/(x2-1)
step 2
    find the y intercept using
    b = mx-y
step 3 
    bruteforce apply every point with others
"""
def slope(p1,p2):
    print(p1,p2,p3)
    x1,y1 = p1
    x2,y2 = p2

    return (y2-y1)/(x2-x1)


def max_points_on_line(pts):
    pts_len = len(pts)
    hash_map = {}
    for p1 in range(pts_len):
        hash_map[i]
        for p2 in range(i+1, pts_len)
            

def main():
    pts = [(1,1),(2,2),(3,3)]
    print(is_line(*pts))

if __name__ == '__main__':
    main()
